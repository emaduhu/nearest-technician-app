name: nearest_technician_app
description: Nearest technician demo with Firebase integration
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.10.0
  firebase_messaging: ^14.4.0
  http: ^0.13.5
  geolocator: ^9.0.2
  permission_handler: ^12.0.1

flutter:
  uses-material-design: true
